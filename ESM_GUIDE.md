# ESM 주문 수집 및 취합

기준: REQM 판매전표 브랜치 `agent/ecount-transfer-integrated-1.0.5`, 커밋 `c3a1e3a`.

## 실행

Windows 10/11 64비트에서 `REQM_판매전표_ESM.exe`를 실행합니다. 자동 수집에는 Microsoft Edge 또는 Google Chrome이 필요합니다. Python은 별도 설치하지 않아도 됩니다. 기존 DB 연결 설정을 사용하는 경우 기존 `config.json`을 실행파일 옆에 두세요. 이 파일과 로그인 비밀번호는 배포 파일에 포함하지 않습니다.

## 자동 수집

1. 판매전표 화면의 **ESM 주문 수집**을 누릅니다.
2. **ESM 로그인**을 누르고 새로 열린 브라우저에서 로그인합니다. 추가 인증은 브라우저에서 직접 완료하세요. 기존 ESM 비밀번호를 앱/파일에 저장하지 않습니다. 창을 닫으면 이 수집용 로그인 세션도 종료됩니다.
3. 조회 기준과 시작일·종료일, 원본 보관 위치를 확인합니다. 주문일·결제완료일·발송예정일·발송일·배송완료일·정산완료일·재배송일 중에서 선택할 수 있습니다.
4. **수집 시작**을 누릅니다. A/G 전체를 선택하고 입금대기·배송대기·배송준비·배송중·배송완료·미수령신고·정산예정·정산완료를 각각 조회합니다. 배송상태의 ‘전체’는 사용하지 않고 세부조건은 ‘전체’를 사용합니다. 긴 기간은 최대 31일씩 나눠 조회합니다.
5. 완료 후 취합 Excel을 저장하거나 **판매전표로 가져오기**를 누릅니다. 가져오기는 현재 분석 대상을 ESM 수집 결과로 교체합니다. 이전 수집 결과에 무조건 이어붙이지 않습니다.

원본 다운로드를 진행할 때 사이트의 개인정보 다운로드 안내를 확인 처리합니다. 로그인 인증, 사이트 응답 지연, 다운로드 제한 또는 화면 변경으로 실패하면 완료로 표시하지 않습니다. 이미 받은 원본은 남고, 같은 기간을 다시 수집할 수 있습니다. 0건인 상태는 기록만 남고 원본 파일은 만들지 않습니다.

## 저장되는 파일

기본 보관 위치는 Windows 문서 폴더의 `REQM/ESM`입니다. 앱에서 변경할 수 있습니다.

```
REQM/ESM/2026-09-09/153000_실행식별자/
  원본/
    01_배송준비.xls
    02_배송중.xls
  수집기록.json
  ESM_취합.xlsx
```

원본은 내려받은 파일의 바이트를 수정하지 않고 보관합니다. 보관 파일명에는 순번·상태를 사용하며 원래 다운로드 파일명, 실제 받은 시각, 조회 기간, 화면 건수, 파일 해시를 수집기록에 남깁니다. 같은 날 반복 실행해도 다른 폴더를 만듭니다. **원본 폴더 열기**, **원본 전체 ZIP 저장**, **취합 Excel 저장**, **이전 수집 열기**를 지원합니다. ZIP은 원본과 수집 기록을 포함합니다. 원본 보관 폴더에는 구매자 개인정보가 포함될 수 있으므로 접근 권한과 보관 기간을 관리하고 공유 전 내용을 확인하세요. ZIP에 비밀번호가 자동으로 설정되지는 않습니다.

이전 수집은 해당 실행 폴더의 `수집기록.json`을 선택합니다. 원본이 변경되었으면 가져오기를 차단합니다. 실패한 실행도 원본 폴더 및 ZIP으로 확인할 수 있지만, 취합/전표 가져오기는 완료된 실행만 가능합니다.

## 기존 Excel 가져오기

**내려받은 ESM 원본 가져오기**에서 XLS/XLSX 여러 개를 선택할 수 있습니다. 선택 파일을 새 실행 폴더에 복사한 후 전체 행을 취합합니다. 이 방식은 화면의 날짜로 행을 필터링하지 않으며, 수집기록에 ‘파일 가져오기 · 기간 필터 미적용’을 표시합니다. 취합 파일이 아닌 주문번호·배송상태가 포함된 ESM 원본을 선택하세요.

## 금액과 중복

취합 Excel에는 아이디, 상품명, 수량, 주문옵션, 판매금액, 판매자쿠폰할인, 개당 금액을 저장합니다. 첫 6개 열은 주황색, 계산 열은 파란색입니다.

**개당 금액 = (판매금액 − 판매자쿠폰할인) ÷ 수량**

금액 문자열의 쉼표를 처리하며, 수량 0·비정수, 빈 금액, 음수 금액, 판매금액보다 큰 쿠폰은 오류로 표시합니다. 미리보기는 최대 소수 8자리, Excel은 계산식을 보존합니다. 전표에는 차감 후 총액을 넘겨 기존 금액 보존 로직으로 처리합니다. 수수료나 배송비, 구매쿠폰·포인트는 추가 차감하지 않습니다. 이번 ESM 전표의 배송비는 0이며 상품금액만 전달합니다.

아이디·주문번호·상품번호·상품명·옵션이 같은 행은 동일 주문 후보입니다. 여러 파일에 완전히 동일한 주문이 있으면 한 번만 반영합니다. 같은 원본 안에 식별정보가 반복되거나, 파일 간 동일 주문의 상태·수량·금액이 다르면 취합을 중단합니다. 상품/옵션이 다른 정상 주문은 별도 유지합니다. 수집 중 주문 상태가 변경된 경우 기간을 다시 수집해 확인하세요.

## 판매전표 연결

계정의 A/G 구분으로 기존 옥션·지마켓 거래처 DB에 연결합니다. 상품/옵션 매칭이 없는 경우 기존 ‘확인 필요’ 기능으로 연결합니다. 기존 창고·거래처별 전표 분리와 금액 차이 검증을 유지합니다. 실제 이카운트 등록은 기존 미리보기와 사용자 확인 절차를 거칩니다. ESM 수집 버튼은 주문 취소·발송·반품 처리나 이카운트 등록을 실행하지 않습니다.

## 검증 범위

합성 주문으로 수량/쿠폰 계산, 원본 바이트 보존, 파일 무결성, 중복/충돌, 건수 불일치, 취합 수식, 거래처 분리, UI 가져오기를 테스트했습니다. 제공된 실제 XLS 15행도 읽기 검증했습니다. ESM 실제 화면에서 계정 선택·날짜 선택·조회·개인정보 안내와 다운로드 이벤트를 확인했습니다. 배포 PC의 로그인 및 8개 상태 전체 자동 수집은 첫 사용 시 조회 건수와 원본을 함께 확인하세요.
